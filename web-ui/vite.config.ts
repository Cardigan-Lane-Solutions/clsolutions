import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  build: {
    outDir: 'build', // keep existing folder name for deployment compatibility
    emptyOutDir: true,
  },
});
